using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum PlayerInput { Active, Disabled }
public enum PlayerState { Default, Dead, Stunned, Invincible }
public enum GameState { Default, Paused }
